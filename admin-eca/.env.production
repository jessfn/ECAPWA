VITE_API_URL=https://api-eca.sembrandodatos.com
# El token real de Mapbox NO se commitea (GitHub lo bloquea como secreto
# aunque sea una clave pk. pública) — vive en `.env.production.local` en
# el servidor (gitignored, `.env.*.local`), que Vite carga con más
# prioridad que este archivo y sobrevive a cada `git pull` del deploy.
# VITE_MAPBOX_TOKEN=pk.tu_token_publico_de_mapbox
